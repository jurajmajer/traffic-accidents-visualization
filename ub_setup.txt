sudo apt-get -y update
sudo apt install git
git clone https://github.com/jurajmajer/traffic-accidents-visualization.git 
sudo chown -R jmajer:jmajer traffic-accidents-visualization/
cd traffic-accidents-visualization/
/opt/Anaconda3-2020.07/bin/python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install gunicorn pymysql
nano .flaskenv
sudo apt install supervisor
sudo nano /etc/supervisor/conf.d/traffic-accidents-visualization.conf
sudo supervisorctl reload
sudo apt install nginx (https://www.digitalocean.com/community/tutorials/how-to-install-nginx-on-ubuntu-20-04)
/opt/traffic-accidents-visualization$ mkdir tls-certificates
openssl req -new -newkey rsa:4096 -days 365 -nodes -x509 -keyout key.pem -out cert.pem
sudo nano /etc/nginx/sites-available/traffic-accidents-visualization
sudo rm /etc/nginx/sites-enabled/default
sudo ln -s /etc/nginx/sites-available/traffic-accidents-visualization /etc/nginx/sites-enabled/
sudo nano /etc/nginx/nginx.conf (uncomment server_names_hash_bucket_size)
sudo nginx -t
sudo systemctl restart nginx

gunicorn -b localhost:8000 -w 4 accidents:app
sudo supervisorctl stop traffic-accidents-visualization
sudo supervisorctl start traffic-accidents-visualization

- Firewall - ufw (https://www.digitalocean.com/community/tutorials/how-to-install-nginx-on-ubuntu-20-04).
	sudo ufw status -> inactive